# Required environment variables for AWS Backend
# Copy this file to .env (if using dotenv) or set these variables in your environment

# The URL where the frontend is deployed (e.g., https://your-domain.com)
VERCEL_DEPLOYMENT_URL=https://your-domain.com

# A shared secret key for securing API communication between frontend and backend
# Generate a strong random string (e.g., `openssl rand -base64 32`)
AWS_API_KEY=your-secret-api-key
